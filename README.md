## Styleguide

### Imports

- Use absolute paths for imports directly from src folder
- Imports order:

* node_modules
* middlewares
* modules
* types
* exceptions
* utils
* all relative paths (e.g. inside module)
